import { Paginator, paginatorOptions } from 'dsc-pagination';

export class AmethystPaginator extends Paginator {
    constructor(options: paginatorOptions) {
        super(options);
    }
}
export type amethystPaginatorOptions = paginatorOptions;
